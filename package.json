{
  "name": "rental-mobile",
  "version": "1.0.0",
  "description": "Website Rental Kendaraan",
  "scripts": {
    "dev": "npx @tailwindcss/cli -i ./input.css -o ./public/css/app.css --watch",
    "build": "npx @tailwindcss/cli -i ./input.css -o ./public/css/app.css --minify",
    "build:js": "node build.js"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.0.0",
    "tailwindcss": "^4.0.0"
  },
  "dependencies": {
    "alpinejs": "^3.14.0",
    "htmx.org": "^2.0.0"
  }
}